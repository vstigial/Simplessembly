global(main);
begin_label(main);
	external(printf);
	
	str_var(db, fmt, "%s %s %s %s %s %s %s %s %s %s %s %s\n");
	str_var(db, msg, "Hello World!");

	call(printf, fmt, msg, msg, msg, msg, msg, msg, msg, msg, msg, msg, msg, msg);

	exit(0);
